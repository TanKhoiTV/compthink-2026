name: Deploy to GitHub Pages (TREKPOLOGY — old codebase)

on:
  push:
    branches: [main]

  # Allow manual trigger from Actions tab
  workflow_dispatch:

# Grant GITHUB_TOKEN permissions for Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write

# Only one deploy at a time
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Install Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: TREKPOLOGY/package-lock.json

      - name: Build TREKPOLOGY frontend
        run: |
          npm ci
          npm run build
        working-directory: TREKPOLOGY

      - name: Patch old client socket URL → old server on HF
        run: |
          # Override the localhost URL to the old-stack HF Space
          OLD_URL='"http://localhost:3001"'
          NEW_URL='"https://khoinguyentran-trekkopoly-old-server.hf.space"'
          sed -i "s|$OLD_URL|$NEW_URL|g" TREKPOLOGY/build/online/socketClient.js
          grep -n 'io("' TREKPOLOGY/build/online/socketClient.js

      - name: Prepare deployment directory
        run: |
          mkdir -p _site
          cp -r TREKPOLOGY/* _site/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

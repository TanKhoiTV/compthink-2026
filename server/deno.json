{
  "tasks": {
    "start": "deno run --allow-net server.ts",
    "dev": "deno run --allow-net --watch server.ts",
    "lint": "deno lint"
  },
  "lint": {
    "rules": {
      "exclude": ["no-unused-vars"]
    }
  },
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true
  }
}
